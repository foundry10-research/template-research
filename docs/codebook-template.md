# Codebook — [PROJECT NAME]

Documentation of every variable in the analytic dataset.

> Rename this file to `codebook.md` once you start filling it in.

A codebook is what lets someone use your data without asking you what
`q17_r3` means. It is also what lets *you* use your data in two years. Fill it
in as you clean, not at the end — reconstructing variable meanings from a
cleaning script is slow and error-prone.

---

## Dataset

| | |
|---|---|
| **File** | `data/processed/analysis_sample.rds` |
| **Generated by** | `code/01_clean.R` |
| **Rows** | [n] — one row per [participant / participant-wave / observation] |
| **Columns** | [n] |
| **Last updated** | [YYYY-MM-DD] |

### Unit of analysis

[One row = one what? Be specific. "One row per participant per wave" is the
kind of sentence that prevents a whole class of analytic error.]

### Sample

- **Recruited:** [n]
- **Excluded:** [n] — [reasons, with a pointer to `decisions.md`]
- **Analytic sample:** [n]

---

## Variables

### Identifiers

| Variable | Type | Description | Values |
|---|---|---|---|
| `participant_id` | character | De-identified participant ID | `P001`–`P250` |
| `wave` | integer | Data collection wave | 1, 2, 3 |

### Demographics

| Variable | Type | Description | Values | Missing |
|---|---|---|---|---|
| `age` | integer | Age in years at enrollment | 13–18 | 4 |
| `grade` | integer | Grade level | 9–12 | 0 |
| `gender` | factor | Self-reported gender | [list every level] | 2 |

### Measures

| Variable | Type | Description | Values | Missing |
|---|---|---|---|---|
| `scale_x_mean` | numeric | Mean of `x1`–`x8`, [scale name] | 1.0–5.0 | 6 |

### Outcomes

| Variable | Type | Description | Values | Missing |
|---|---|---|---|---|
| `outcome` | numeric | [description] | | |

---

## Scales

For each composite, record what it is made of so someone can verify or
reconstruct it.

### [Scale name]

- **Source:** [Author, year, and the full citation]
- **Items:** `x1`–`x8`
- **Response scale:** 1 = Strongly disagree … 5 = Strongly agree
- **Reverse-coded items:** `x3`, `x7` — reversed in `code/01_clean.R`
- **Scoring:** Mean of all 8 items; computed only when at least 6 are non-missing
- **Reliability in this sample:** α = [value]
- **Modifications from the original:** [any wording changes, and why]

Item wording belongs in `materials/`.

---

## Derived variables

Anything computed rather than collected. Give the formula, not a description of
the formula.

| Variable | Derived from | How |
|---|---|---|
| `age_centered` | `age` | `age - mean(age, na.rm = TRUE)` |

---

## Missing data

- **Codes in the raw data:** [e.g. `-99` = prefer not to answer, `-88` = not
  applicable, blank = skipped]
- **How converted:** [where in `01_clean.R`]
- **Pattern:** [MCAR / MAR / not assessed]
- **Handling in analysis:** [listwise deletion / multiple imputation / FIML]

---

## Known issues

The things a new analyst would otherwise discover the hard way.

- [e.g. "Wave 2 was collected during a school closure; response rate was 40% versus 85% in waves 1 and 3."]
- [e.g. "Items 12–14 had a formatting error in the first 30 responses; see `decisions.md`, 2026-04-02."]

---

## Provenance

| | |
|---|---|
| **Collected** | [dates] |
| **Instrument** | [Qualtrics / REDCap / paper] |
| **Raw file** | [filename] |
| **De-identified by** | [who, when, and what was removed] |
