# Processed data

Analysis-ready files generated by `code/01_clean.R`. Safe to delete — running `code/run_all.R` recreates everything here.

Files in this folder are not tracked by Git. If you have a file here that cannot be regenerated from `data/raw/` plus the code in `code/`, then a step is missing from the code and the project is not reproducible.
